func main() {
    let counter = 0
    let bump = fn() {
        counter = counter + 10
        return counter
    }
    print(bump())
    print(bump())
    print(counter)

    let base = 100
    let addBase = fn(x) {
        return base + x
    }
    print(addBase(5))
    print(addBase(50))
}
