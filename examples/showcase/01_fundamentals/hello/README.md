# 01 Fundamentals — Hello World

STATUS: WORKING TODAY (validated with `karkain check` + `karkain run`,
default kcc engine).

## What it demonstrates

- Hello World, `print`, string concatenation
- dynamic typing (`let`) with an explicit `float` annotation (`let rate: float = 2.5`)
- if / else-if / else, `while`, recursion (`fib`), arrays, `len`, `push`, indexing

## Commands

```
karkain check examples/showcase/01_fundamentals/hello/main.kark
karkain run   examples/showcase/01_fundamentals/hello/main.kark
```

## Expected output (verified)

```
Hello, Karkain!
13
10
Karkain v2
flag set
negative
zero
positive
1
4
9
16
25
55
5
52
9
```