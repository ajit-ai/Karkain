# Phase 115 — Developer Preview Readiness: Audit Report

## Status: PASS

This document is the evidence-based audit behind the Phase 115 Developer
Preview gate. Every checklist item in
`docs/release/developer-preview-checklist.md` maps to a concrete gate or
verification recorded here. The paired completion report is
`docs/audit/PHASE-115-FINAL-REPORT.md`.

## Gate scope

| # | Checklist section | Evidence | Result |
|---|-------------------|----------|--------|
| 1 | Obtainable | `git ls-remote https://github.com/ajit-ai/Karkain.git` OK; repo is the `origin` remote of this tree | PASS |
| 2 | Buildable | `go build ./...` exit 0; bootstrap `go build -o karkain.exe ./cmd/karkain/` OK | PASS |
| 3 | Usable | Fresh-checkout simulation `TestPhase115_FreshCheckout` (clone -> build -> run/check/build/test hello) 81.23 s | PASS |
| 4 | Honest labels | Docs status vocabulary (`docs/source/status/`), example `// Status:`/`// Engine:` headers, no `v1.0.0` claims | PASS |
| 5 | Baseline validation | `go vet ./...`, `go build ./...`, regression suites below | PASS |
| 6 | Error experience | Before/after audit (below); friendly typo hint + missing-file diagnostic | PASS |
| 7 | Documentation | Strict Sphinx `-W --keep-going` 0 warnings; `linkcheck` clean | PASS |
| 8 | Non-applicable features | Labeled in docs as Experimental/Planned; gated at code level (K108 wasm gates, `@simd`, etc.) | PASS |
| 9 | Release notes | `docs/source/release-notes.rst` (v0.115.0, anti-1.0 rule) | PASS |
| 10 | Contribution guide | `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md` | PASS |
| 11 | License | `LICENSE` (MIT, "Copyright (c) 2025-2026 The Karkain Contributors") | PASS |
| 12 | CI review | `.github/workflows/ci.yml`: vet step, phase-114/115 gates, extra doc pages | PASS |
| 13 | Feedback channel | GitHub Issues referenced in README/docs/contributing | PASS |
| 14 | Public status | `🚀 Karkain Developer Preview (v0.115.0)` in README, docs index, AGENTS | PASS |
| 15 | Version identity | `--version` reports `Karkain Compiler v0.115.0 (…, Developer Preview Build)` | PASS |
| 16 | No 1.0 claims | Grep sweep; historical Phase 90/91 records marked superseded | PASS |

## Error-experience audit (before / after)

Probed with real invocations on freshly built binaries.

| Case | Before | After |
|------|--------|-------|
| `karkain build` | exit 2, usage | unchanged (exit 2) |
| `karkain buidl x.kark` | exit 2, bare unknown-command error | exit 2, `Did you mean: karkain build` |
| `karkain run missing.kark` | exit 1, raw `os` error text | exit 2, `Error: cannot open 'missing.kark': file not found` |
| `karkain run --target bogus` | exit 2 | unchanged (exit 2) |
| syntax error in file | exit 3, `error[K001]` with excerpt | unchanged |
| valid hello | exit 0, `Hello, Karkain!` | unchanged |

Implementation: `suggestCommand`/`editDistance` hint inside `cli.ValidateKarFile`
(`pkg/cli/commands.go`) and a pre-dispatch existence check in
`cmd/karkain/main.go` after `ValidateKarFile`.

## Version-identity sweep

Every stale compiler version string was found and corrected:

- `cmd/karkain/main.go:16` and `pkg/cli/commands.go:23` now
  `Karkain Compiler v0.115.0 (%s/%s, Developer Preview Build)`.
- `src/compiler/main.kark` (three `--version`/usage/LSP strings) — self-hosted
  kcc reports the same identity.
- `src/compiler/codegen.kark` — all six "Generated by" headers.
- Go backends `pkg/codegen/{dwarf,qasm,qec,qir,openpulse,quantum_opt}.go`.
- `docs/source/tools/cli.rst` example, `docs/source/conf.py` footer default,
  new root `VERSION` file, `SPEC.md` header + history table,
  `examples/showcase/README.md`, `scripts/release-all.{sh,ps1}` defaults.
- Tests updated: `pkg/bootstrap/args_test.go`, `pkg/cli/phase88_test.go`,
  `pkg/cli/phase90_test.go`. A stale root `karkain.exe` (old ldflags build)
  shadowed `go build` in those gates and was deleted.

## Gates executed

| Gate | Result | Time |
|------|--------|------|
| `TestPhase115_*` (6: version, help, skeleton, doc tree, example inventory, fresh checkout) | PASS | 92.9 s |
| `TestPhase115_FreshCheckout` (clone -> build -> run/check/build/test) | PASS | 81.2 s |
| `TestPhase115_RepoSkeleton` (integrity files + README link resolution) | PASS | 0.1 s |
| `TestPhase114_CorpusExamples_GoEngine` + `_CorpusCoverage` | PASS | in 99.8 s batch |
| `TestPhase114_CorpusExamples_KCCParity` | PASS | 181.0 s |
| `TestPhase88_SelfHostedCompiler` + `TestPhase90` (version gates) | PASS | 151.2 s |
| `TestPhase99` (self-hosted checks incl. compiler-sources type-check) | PASS | 80.5 s |
| `TestCLI_ValidateKarFile` / check / run / phase 100 / phase 111 | PASS | in 99.8 s batch |
| `pkg/bootstrap TestArgs` (version identity) | PASS | 15.8 s |
| `pkg/codegen/...` full | PASS | 42.0 s |
| `pkg/lexer/... pkg/parser/... pkg/pm/...` | PASS | earlier run |
| `pkg/sema pkg/target pkg/wasm pkg/compiler pkg/runtime` | PASS | earlier run |
| `go build ./...` / `go vet ./...` | PASS | 0/0 |

## Documentation

- `python -m sphinx -b html docs/source docs/build/html -W --keep-going`
  -> `build succeeded` (0 warnings), including `release-notes`/
  `example-matrix` pages added to the required-pages CI check.
- `python -m sphinx -b linkcheck` -> clean; the two canonical GitHub URLs are
  in `linkcheck_ignore` because GitHub serves 404 to link-checker clients even
  for the public repository root (verified: `git ls-remote` works, web/API
  both 404 for the anonymous bot on this host).

## Boundaries (documented, not defects)

- Live public-visibility of `https://github.com/ajit-ai/Karkain` cannot be
  confirmed from this host (GitHub bot-blocking returns 404 for web/API);
  git access is verified working.
- `linkcheck` ignores the two canonical GitHub URLs (see above).
- The Phase 88/90/91 audit reports remain historical records and are
  explicitly marked superseded rather than rewritten.

## Revision bookmark

- Gate commit (develop): `develop` HEAD after Phase 115 merge.
- Final merge to `main`: see `PHASE-115-FINAL-REPORT.md` for hashes.